<%= form_for([@persona, @persona.historial_clinicos.build]) do |f| %>
  <% if @persona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@persona.errors.count, "error") %> prohibited this historial_clinico from being saved:</h2>

      <ul>
      <% @persona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <td><%= f.text_field :fecha, 'data-behaviour' => 'datepicker'%></td>
  <td><%= f.collection_select :fk_id_lookup, data_for_select("EstadoPaciente"), :id, :value%></td>
  <td><%= f.submit 'Guardar', class: "btn btn-primary"%></td>
<% end %>
